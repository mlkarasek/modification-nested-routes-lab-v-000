<h1>Create an Artist</h1>

<%= link_to "New Song", new_artist_song_path(@artist) %>

<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />

  <%= f.submit %>
<% end %>
